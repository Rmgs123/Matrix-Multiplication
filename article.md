# Перемножение матриц
**Группа 1 - Практические методы: тройной цикл, алгоритм Штрассена, вариант Штрассена–Винограда**

---

## Введение

Умножение матриц — центральная операция численных вычислений: от решения систем линейных уравнений и методов наименьших квадратов до компьютерной графики и глубокого обучения.

Пусть даны две матрицы:

- $A \in \mathbb{R}^{n \times m}$,
- $B \in \mathbb{R}^{m \times p}$.

Их произведение $C = A B$ имеет размер $n \times p$, элементы определяются по формуле:

$$
c_{ij} = \sum_{r=1}^{m} a_{ir} \, b_{rj}, \qquad i = 1,\dots,n,\; j = 1,\dots,p.
$$

Наивная реализация этой формулы через тройной цикл даёт
$n \cdot m \cdot p$ умножений и примерно столько же сложений,  
то есть трудоёмкость порядка $O(n m p)$.  
В квадратном случае $n=m=p$ это даёт $O(n^3)$.

В этой статье собраны **практические, точные** алгоритмы:

- классический тройной цикл;
- алгоритм Штрассена (1969);
- вариант Штрассена–Винограда (1971).

Все они корректны над полями и кольцами (то есть с аккуратной целочисленной или рациональной арифметикой). При работе с числами с плавающей запятой нужно дополнительно думать про накопление ошибок.

---

## 1. Классический алгоритм (тройной цикл)

### 1.1. История и идея

Это прямое следование определению умножения матриц.  
Идея простая: каждый элемент результата — это **скалярное произведение** строки из $A$ и столбца из $B$.

Плюсы:

- простота;
- численная устойчивость (в floating point);
- хорошая локальность данных — его легко оптимизировать для кэша (блочное умножение, векторизация, параллелизм).

Минусы:

- асимптотика $O(n^3)$ в квадратном случае.

### 1.2. Сложность и операции

**Общий случай $n \times m$ на $m \times p$:**

- умножений: $n \cdot m \cdot p$;
- сложений: $n \cdot p \cdot (m - 1)$.

**Квадратный случай $n \times n$:**

- $n^3$ умножений;
- $n^3 - n^2$ сложений;
- асимптотика: $O(n^3)$.

### 1.3. Пример $3\times 3$

Возьмём:

$$
A=\begin{pmatrix}
2 & -1 & 3\\
0 & 5 & 1\\
4 & 2 & 1
\end{pmatrix},\quad
B=\begin{pmatrix}
1 & 4 & -2\\
3 & -1 & 0\\
2 & 5 & 1
\end{pmatrix}.
$$

Посчитаем пару элементов явно (скалярные произведения строки на столбец):

- элемент $c_{12}$ — 1-я строка, 2-й столбец:
  $$c_{12} = (2,-1,3)\cdot(4,-1,5) = 2\cdot4 + (-1)\cdot(-1) + 3\cdot5 = 8 + 1 + 15 = 24.$$

- элемент $c_{33}$ — 3-я строка, 3-й столбец:
  $$c_{33} = (4,2,1)\cdot(-2,0,1) = 4\cdot(-2) + 2\cdot 0 + 1\cdot 1 = -8 + 0 + 1 = -7.$$

Полный результат (остальные элементы считаются аналогично):

$$
C = AB =
\begin{pmatrix}
5 & 24 & -1\\
17 & 0 & 1\\
12 & 19 & -7
\end{pmatrix}.
$$

### 1.4. Численные и практические аспекты

- при обычных вычислениях с плавающей запятой метод **численно устойчив**:
  ошибки округления растут умеренно;
- при малых и средних размерах, а также в реализациях уровня BLAS (C/Fortran с блочностью и SIMD) тройной цикл (в грамотной блочной реализации) часто оказывается **быстрее** более сложных алгоритмов, несмотря на худшую асимптотику.

Для многих практических задач «на земле» классический алгоритм — это и есть основной рабочий инструмент.  
Быстрые алгоритмы (Штрассен и далее) становятся полезны при достаточно больших размерах и при грамотной реализации.

---

## 2. Алгоритм Штрассена (1969)

### 2.1. Идея и асимптотика

Ф. Штрассен в 1969 году показал, что можно умножать матрицы **быстрее, чем $O(n^3)$**.

Идея:

- рекурсивно разбивать матрицу $A$ и $B$ на блоки $2\times 2$;
- научиться умножать два блока $2\times 2$ **семью умножениями** вместо восьми
  за счёт дополнительных сложений/вычитаний.

Если размер матрицы $n$ — степень двойки, и на каждом шаге мы делим её пополам, то получаем рекурренту:

$$
T(n) = 7T\left(\frac{n}{2}\right) + O(n^2),
$$

откуда

$$
T(n)=O\!\bigl(n^{\log_2 7}\bigr)\approx O(n^{2{,}8074}).
$$

То есть показатель степени становится меньше трёх.

### 2.2. Формулы для блока $2\times 2$

> 🧊 **Блок-схема Штрассена для $2\times 2$ матриц**

Пусть

<p align="center">

$$
A = \begin{pmatrix}
a & b\\
c & d
\end{pmatrix},
\quad
B = \begin{pmatrix}
e & f\\
g & h
\end{pmatrix}.
$$

</p>

Введём семь промежуточных величин:

$$
\begin{aligned}
M_1 &= (a+d)(e+h), &
M_2 &= (c+d)e, &
M_3 &= a(f-h),\\
M_4 &= d(g-e), &
M_5 &= (a+b)h, &
M_6 &= (c-a)(e+f),\\
M_7 &= (b-d)(g+h).
\end{aligned}
$$

Тогда элементы $C = AB$ выражаются через $M_k$:

$$
\begin{aligned}
C_{11}&=M_1 + M_4 - M_5 + M_7, &
C_{12}&=M_3 + M_5,\\
C_{21}&=M_2 + M_4, &
C_{22}&=M_1 - M_2 + M_3 + M_6.
\end{aligned}
$$

Замена «8 умножений + чуть-чуть сложений» на «7 умножений + больше сложений» и лежит в основе ускорения.

### 2.3. Короткий числовой пример $2\times 2$

Пусть

$$
A=
\begin{pmatrix}
1 & 3\\
7 & 5
\end{pmatrix},\quad
B=
\begin{pmatrix}
6 & 8\\
4 & 2
\end{pmatrix}.
$$

Можно руками посчитать $M_1,\dots,M_7$, затем $C_{11},\dots,C_{22}$ по формулам выше и убедиться, что получится тот же результат, что и при обычном умножении. Важно, что **схема даёт точно то же самое $C$**, просто через другую последовательность операций.

### 2.4. Как это работает для нечётного размера (пример $3\times 3$)

Алгоритм Штрассена естественно описывается для квадратных матриц размера $2^k \times 2^k$.  
Что делать, если размер нечётный (например, $3\times 3$)?

Один стандартный трюк:

1. **Дописать нулевые строки и столбцы** до ближайшей степени двойки сверху.  
   Для $3\times 3$ — до $4\times 4$.

2. Применить алгоритм к расширенным матрицам $A', B'$.

3. Взять верхний левый блок результата нужного размера и отбросить «хвосты» с нулями.

В примере из раздела 1.3, если мы допишем к $A$ и $B$ нулевую строку и нулевой столбец, получим матрицы $A', B' \in \mathbb{R}^{4\times 4}$. Их можно разбить на блоки $2\times 2$, вычислить 7 блоковых произведений $M_1,\dots,M_7$, собрать блоки $C_{11}, C_{12}, C_{21}, C_{22}$, а затем отбросить последний ряд и последний столбец. Итог будет тот же, что и при классическом умножении $3\times 3$.

Этот приём легко обобщается на большие размеры (например, с 513 до 1024), но у него есть **важный недостаток**, о котором будет отдельный раздел: иногда вы серьёзно переплачиваете за нули.

---

## 3. Вариант Штрассена–Винограда (1971)

### 3.1. Идея: меньше сложений при тех же 7 умножениях

Штрассен–Виноград — это модификация, предложенная Ш. Виноградом в 1971 году. Основные моменты:

- количество умножений **на шаг рекурсии остаётся 7**, как у Штрассена;
- количество **дополнительных сложений/вычитаний блоков** сокращается (в теории с $\approx 18$ до $\approx 15$ для блоков размера $n/2$);
- асимптотика остаётся той же:  
  $O(n^{\log_2 7})$.

Смысл: уменьшить константу и чуть облегчить нагрузку на память.

### 3.2. Конкретные формулы с $S_i$ и $P_i$

Часто в учебной литературе (и в этой статье) приводится следующий «канонический» вариант.

Пусть мы работаем с блочным разбиением:

$$
A = \begin{pmatrix}
A_{11} & A_{12}\\
A_{21} & A_{22}
\end{pmatrix},\quad
B = \begin{pmatrix}
B_{11} & B_{12}\\
B_{21} & B_{22}
\end{pmatrix}.
$$

> 🧊 **Формулы варианта Штрассена–Винограда (вариант с $S_1,\dots,S_{10}$)**

Сначала считаем промежуточные суммы/разности:

$$
\begin{aligned}
S_1 &= B_{12} - B_{22}, &
S_2 &= A_{11} + A_{12}, &
S_3 &= A_{21} + A_{22}, &
S_4 &= B_{21} - B_{11},\\
S_5 &= A_{11} + A_{22}, &
S_6 &= B_{11} + B_{22}, &
S_7 &= A_{12} - A_{22}, &
S_8 &= B_{21} + B_{22},\\
S_9 &= A_{11} - A_{21}, &
S_{10} &= B_{11} + B_{12}. &&
\end{aligned}
$$

Затем 7 блочных произведений:

$$
\begin{aligned}
P_1 &= A_{11} S_1, &
P_2 &= S_2 B_{22}, &
P_3 &= S_3 B_{11}, &
P_4 &= A_{22} S_4,\\
P_5 &= S_5 S_6, &
P_6 &= S_7 S_8, &
P_7 &= S_9 S_{10}. &&
\end{aligned}
$$

И, наконец, собираем блоки результата:

$$
\begin{aligned}
C_{11} &= P_5 + P_4 - P_2 + P_6, &
C_{12} &= P_1 + P_2,\\
C_{21} &= P_3 + P_4, &
C_{22} &= P_5 + P_1 - P_3 - P_7.
\end{aligned}
$$

Здесь $A_{ij}, B_{ij}, C_{ij}, S_k, P_k$ — всё квадратные блоки одного размера (обычно $n/2 \times n/2$).

### 3.3. Сколько сложений на самом деле?

Вот важный момент, который часто вызывает путаницу:

- В оригинальном алгоритме Штрассена (через $M_1,\dots,M_7$):
  - на подготовку аргументов $M_k$ уходит **10** блочных сложений/вычитаний,
  - на сборку блоков $C_{ij}$ — ещё примерно **8**,
  - всего около **18** блочных сложений на уровень рекурсии.

- В варианте с $S_1,\dots,S_{10}$ (который мы написали выше):
  - предварительных $S_i$ — ровно **10** (каждый — одно блочное сложение/вычитание),
  - при честной прямой реализации формул для $C_{ij}$ получается ещё **7–8** сложений/вычитаний,
  - итого получается порядка **17** блочных сложений.

То самое число «15», которое часто встречается в таблицах, относится к:

1. либо **другим, более агрессивным схемам** (где набор промежуточных величин отличается от $S_1,\dots,S_{10}$);
2. либо к случаям, когда мы **очень аккуратно переиспользуем промежуточные суммы** и считаем каждую операцию сложения блоков только один раз в хорошо оптимизированном расписании.

В нашей статье формулы выбраны **учебные и читаемые**.  
Они уже реально экономят несколько сложений по сравнению с «наивным» Штрассеном, но **не реализуют строгий минимум 15**. Поэтому честно можно говорить: «около 17 вместо 18».

В практической Python-реализации (где основная цена — это создание новых матриц/списков и рекурсия) разница между 17 и 18 блок-сложений часто тонет в накладных расходах интерпретатора. Но для теории алгоритмов важно, что существует семейство схем, где количество сложений асимптотически меньше.

### 3.4. Корректность и пример

Если взять те же матрицы $A$ и $B$, что в примере со Штрассеном, дописать их нулями до размера $4\times 4$ и разбить на блоки $2\times 2$, то схема с $S_1,\dots,S_{10}$ и $P_1,\dots,P_7$ даст точно тот же результат:

$$
C =
\begin{pmatrix}
5 & 24 & -1\\
17 & 0 & 1\\
12 & 19 & -7
\end{pmatrix}.
$$

Различие со схемой Штрассена — только в порядке и количестве промежуточных сложений, умножений всё равно 7.

---

## 4. Теория vs практика: асимптотика и реальные времена

### 4.1. Теоретическая сложность (квадратные матрицы)

Если считать **только число арифметических операций** и рассматривать квадратный случай $n \times n$, то:

- классический алгоритм:

$$
T_{\text{classic}}(n) = O(n^3)
$$

- Штрассен и Штрассен–Виноград:

$$
T_{\text{Strassen}}(n) = T_{\text{Winograd}}(n)
  = O(n^{\log_2 7}) \approx O(n^{2.8074})
$$

На больших $n$ функция $n^{2.8074}$ растёт ощутимо медленнее, чем $n^3$, поэтому в пределе быстрые алгоритмы выигрывают.

### 4.2. Почему на практике всё не так просто

В реальной жизни мы работаем не с **абстрактными операциями**, а с:

- кэшами и иерархией памяти;
- ограничениями по памяти (куча временных матриц в рекурсии);
- интерпретаторами (например, Python), где даже простое сложение матриц — это много скрытых операций.

Типичная картинка (упрощённо, пример на чистом Python):

- при маленьких размерностях (например, $n \le 64$) тройной цикл и Штрассен показывают **один и тот же порядок** времени, а иногда тройной цикл быстрее;
- начиная с некоторого порога (где-то в районе $n=200\dots 300$ для «чистого» Python) аккуратно реализованный Штрассен/Штрассена–Винограда начинает выигрывать;
- для очень больших $n$ (сотни/тысячи) субкубическая асимптотика уже чувствуется.

Из этого вытекает стандартная практика: реализуют **гибридный алгоритм**, который:

- использует Штрассена только до некоторой глубины рекурсии;
- ниже определённого порога $n_0$ (например, 32, 64, 128) переходит на обычный тройной цикл.

---

## 5. Прямоугольные матрицы, дополнение нулями и «неудобные» размеры

До этого мы в основном говорили про квадратные матрицы $n \times n$.  
Но в реальных задачах часто встречается произведение:

- $A$ размера $n \times m$,
- $B$ размера $m \times p$,
- результат $C$ размера $n \times p$.

### 5.1. Классический алгоритм легко обобщается

Для тройного цикла всё тривиально:

$$
c_{ij} = \sum_{r=1}^{m} a_{ir} b_{rj}, \quad
i=1,\dots,n,\ j=1,\dots,p.
$$

Итерационные индексы: $i$ по строкам $A$, $k$ по «общей размерности» $m$, $j$ по столбцам $B$.

Тут **не требуется никаких дополнений нулями** — алгоритм естественно работает для любых $n, m, p$.

### 5.2. Штрассен для прямоугольных матриц

Чистый Штрассен формально определён для случаев:

- $A$ и $B$ квадратные, одного размера;
- размер удобно делится пополам.

Чтобы распространять идею на другие случаи, делают одно из:

1. **Встраивают прямоугольный случай в квадратный**:
   - берут $s = \max\{n, m, p\}$;
   - подбирают ближайшую сверху степень двойки $q = 2^k \ge s$;
   - создают матрицы $A'$ размера $q \times q$ и $B'$ размера $q \times q$,  
     где исходные матрицы скопированы в левый верхний угол, а остальное заполнено нулями;
   - применяют Штрассена к $A', B'$;
   - берут левый верхний блок результата нужного размера $n \times p$.

2. **Используют гибридное разбиение и частично прямоугольные блоки**, но это сильно усложняет реализацию и обычно не делается в учебных проектах.

На практике чаще всего используют **вариант (1)** плюс гибрид с тройным циклом.

### 5.3. Почему дополнение нулями может «убивать» выигрыш

Дополнение до степени двойки — это удобно для рекурсии, но может быть **сильно невыгодно**, если размер «чуть не дотягивает».

Например:

- было $n = 384$;
- ближайшая сверху степень двойки: $512$.

При дополнении матриц до размера $512 \times 512$ мы:

- увеличиваем количество элементов в $\left(\frac{512}{384}\right)^2 \approx 1.78$ раза;
- платим за кучу умножений на ноль, плюс за лишние уровни рекурсии.

В эксперименте (чистый Python) как раз наблюдается ситуация:

- при $n = 384$ наивный тройной цикл $O(n^3)$ показывает время порядка ~4 секунд;
- а наивная реализация Штрассена с дополнением до $512\times512$ может тратить ~6+ секунд и **проигрывать**.

Отсюда важное практическое следствие:

> **Гибридный алгоритм лучше, чем «строгий» Штрассен с обязательным дополнением до степени двойки.**

Одна из разумных стратегий:

- рекурсивно применять Штрассена **только когда:**
  - размер достаточно большой,
  - и делится пополам без особых проблем (например, чётный);
- для **нечётных и «плохих» размеров** (сильно отличающихся от степени двойки) **переключаться на обычный тройной цикл** без дополнения.

Такой подход позволяет:

- сохранить выигрыш на «красивых» размерах (256, 512 и т.п.),
- не сильно проигрывать на «неудобных» вроде 257 или 384.

---

## 6. Сводная таблица методов (группа 1)

| Метод                        | Асимптотика (квадр. случай)                                   | Оценка операций на уровне рекурсии (квадр. блоки)                                           | Ключевая идея / примечания                                                                                                                                      |
|------------------------------|---------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Классический тройной цикл    | $O(n^3)$                                                 | Для $n\times n$: $n^3$ умножений, $n^3-n^2$ сложений                                                                              | Прямое следование определению. Прост, устойчив, хорошо оптимизируется. В BLAS/NumPy-библиотеках доминирует на малых и средних размерностях.                    |
| Штрассена (1969)             | $O(n^{\log_2 7}) \approx O(n^{2.8074})$            | На одном уровне: **7** умножений блоков размера $n/2$, около **18** блочных сложений (подготовка + сборка)                       | Разбиение на $2\times 2$ блоки, семью умножениями вместо восьми. Выигрывает на достаточно больших $n$. Требует аккуратного управления памятью и порогом.       |
| Штрассена–Винограда (1971)   | $O(n^{\log_2 7})$                                       | На одном уровне: **7** умножений блоков $n/2$, порядка **17** блочных сложений для учебной схемы с $S_1,\dots,S_{10}$           | Оптимизирует число сложений по сравнению с оригинальным Штрассеном. Теоретический минимум схем этого типа — ~15 сложений, но приведённые формулы дают ~17.    |

---

## 📁 Реализации алгоритмов (папка code/)

**В директории code/ находятся минимальные, «прямые» реализации всех трёх алгоритмов умножения матриц:**

- classic.md — классический тройной цикл 

- strassen.md — чистый алгоритм Штрассена;

- strassen_winograd.md — вариант Штрассена–Винограда.

**Все реализации:**

- полностью написаны на чистом Python, без внешних библиотек;

- используют базовый случай: рекурсивная база — умножение скаляров;

- содержат только код и краткое текстовое описание — без оптимизаций, без гибридов, без настроек cutoff.

Эти файлы можно использовать как учебные эталонные примеры, а также как основу для собственных оптимизированных или гибридных реализаций.

---

**Группа 2 - Приближенные алгоритмы**

## 7. Приближённый алгоритм умножения матриц (Бини и др., 1979)

### 7.1 Билинейные алгоритмы умножения матриц: формальное определение

Пусть даны матрицы $A$ размером $l\times m$ и $B$ размером $m\times n$. Их произведение $C = A\cdot B$ есть матрица $C$ размера $l\times n$ с элементами $c_{ij} = \sum_{k=1}^m a_{ik} b_{kj}$. Эта операция является билинейной формой относительно элементов $A$ и $B$. Билинейным алгоритмом ранга $r$ для умножения таких матриц ,задача $(l,m,n)$, называется способ вычислить все $l n$ произведений $c_{ij}$ посредством $r$ скалярных умножений и линейных комбинаций входных данных. Формально это означает представление элементов результата в виде:
$$c_{ij} = \sum_{t=1}^{r} \alpha_{it} \beta_{jt} p_t,$$ где каждое $p_t$ получается как скалярное произведение некоторых линейных форм от элементов $A$ и $B$:

$$p_t = \left( \sum_{p,q} u_{pq}^{(t)} a_{pq} \right) \left( \sum_{p,q} v_{pq}^{(t)} b_{pq} \right).$$

Здесь коэффициенты $\alpha_{it}, \beta_{jt}, u_{pq}^{(t)}, v_{pq}^{(t)}$ принадлежат базовому полю (например, $\mathbb{R}$) и определяют фиксированные линейные комбинации. Таким образом, билинейный алгоритм осуществляет $r$ умножений линейных комбинаций элементов $A$ и $B$, а затем линейно комбинирует результаты $p_t$ для получения всех компонент $c_{ij}$. Минимальное возможное $r$ называется билинейной сложностью (или рангом тензора) задачи умножения $(l,m,n)$. Например, классический алгоритм даёт ранг $r = l m n$ (перемножение всех пар элементов с последующим суммированием), а алгоритм Штрассена для $2\times2$-матриц использует $r=7$ вместо 8

## 8. Точные и приближённые билинейные алгоритмы (EC vs APA)

Если билинейный алгоритм дает точное равенство компонент $C = A\cdot B$ при конечном числе операций, будем называть его точно вычисляющим алгоритмом. В этом случае все коэффициенты линейных комбинаций можно выбрать рациональными или целыми числами, и результат получается точно за конечное число скалярных умножений, без привлечения предельных процессов. Классический пример – алгоритм Штрассена: он точно вычисляет произведение $2\times2$ матриц за 7 умножений. Лучшие известные на 1979 г. точные схемы для $n\times n$-матриц имели сложность $O(n^{2.7951})$ (алгоритм Панна). В 1979 году было предложено новое понятие приближённых билинейных алгоритмов, позволяющих достичь меньшей асимптотической сложности ценой произвольно малого вычислительного ошибки. Приближённый билинейный алгоритм (англ. Arbitrary-Precision Approximating algorithm, APA-алгоритм) не требует строгого равенства $C=A\cdot B$ – вместо этого он воспроизводит заданную билинейную форму с заданной точностью, которая может улучшаться до произвольно высокой, если разрешить использование операций с очень большими или малыми коэффициентами (например, зависящими от параметра $\varepsilon$). Формально APA-алгоритм можно описать введением малого параметра $\varepsilon$ в коэффициенты линейных комбинаций: выбираются матрицы $U(\varepsilon)$, $V(\varepsilon)$, $W(\varepsilon)$ размерностей $l\times r$, $m\times r$, $n\times r$ соответственно, такие что выполняется тождество:

$$c_{ij}(\epsilon) = \sum_{t=1}^{r} U_{it}(\epsilon) V_{jt}(\epsilon) W_{ij,t}(\epsilon),$$

где при $\varepsilon\to 0$ коэффициенты $U_{i t}(\varepsilon),V_{j t}(\varepsilon),W_{ij,t}(\varepsilon)$ стремятся к конечным пределам, а суммарная погрешность $\sum_t U_{i t}(\varepsilon)V_{j t}(\varepsilon)W_{ij,t}(\varepsilon) - c_{ij}$ стремится к нулю. Другими словами, за $r$ умножений алгоритм получается вычислить $C$ приближённо, с ошибкой порядка $\varepsilon$ (или $\varepsilon^d$ для некоторых $d>0$) при малом $\varepsilon$. В пределе $\varepsilon\to 0$ восстанавливается точное равенство, хотя для конечного ненулевого $\varepsilon$ результат слегка неточен. При этом величину ошибки можно сделать сколь угодно малой, увеличивая разрядность чисел (либо работая символически с формальным параметром $\varepsilon$ и учитывая главный член разложения).

Важно, что APA-алгоритм допускает принципиально меньшее число умножений $r$ по сравнению с лучшими точными алгоритмами. Однако цена этого – усложнение структуры алгоритма и потенциальные проблемы с численной стабильностью и битовой сложностью. Концепция APA-алгоритмов тесно связана с понятием пограничного ранга (border rank) тензора: фактически $r$ в APA-алгоритме соответствует наименьшему рангу, которого можно достичь в пределах замыкания множества точных алгоритмов (при допущении сколь угодно малых возмущений в тензоре целевой билинейной формы). Если точный ранг задачи – это минимальное $r$ для абсолютно точного представления, то пограничный ранг – минимальное $r$ для предельного представления с $\varepsilon \to 0$. Например, для перемножения произвольных $2\times2$ матриц точный ранг равен 7, и как недавно доказано, пограничный ранг также равен 7. Но если наложить на матрицы дополнительные условия (структурные нули), пограничный ранг может снижаться. Бини и соавт. первыми применили идею пограничного снижения ранга к задаче умножения матриц, предложив APA-алгоритм с экспонентой сложности $\omega \approx 2.7799$

### 8.1 APA-алгоритм для специального случая $2\times2$-матриц (5 умножений)

Ключевой шаг к снижению экспоненты – нахождение нетривиального приближённого алгоритма для малого базового случая. Бини с соавторами рассмотрели произведение особых $2\times2$ матриц вида:

$$A = \begin{pmatrix} 
a & b \\ 
0 & c 
\end{pmatrix}, 
\quad B = \begin{pmatrix} d & e \\ 
f & g 
\end{pmatrix},$$

то есть $A$ – верхняя треугольная матрица, а $B$ – произвольная полная матрица. Их произведение $C = A\cdot B$ имеет вид:

$$C = \begin{pmatrix} ad + bf & ae + bg \\
0 \cdot d + cf & 0 \cdot e + cg 
\end{pmatrix} = \begin{pmatrix} ad + bf & ae + bg \\ 
cf & cg 
\end{pmatrix}.$$

На первый взгляд, для вычисления четырёх неизвестных элементов $c_{11},c_{12},c_{21},c_{22}$ требуется 6 умножений (по числу отдельных произведений $a d$, $b f$, $a e$, $b g$, $c f$, $c g$). Однако, как показали авторы, существует APA-алгоритм, выполняющий эту операцию за $r=5$ умножений. Это означает, что тензор, соответствующий этой частично разреженной $2\times2$ задаче, имеет пограничный ранг не более 5.

Приводится явная конструкция такого алгоритма посредством матриц $U(\varepsilon), V(\varepsilon), W(\varepsilon)$. В качестве иллюстрации опишем схему вычислений качественно. Чтобы снизить число умножений, приближённый алгоритм должен научиться получать сразу несколько требуемых произведений комбинацией одного умножения. Например, заметим, что нижняя строка результата $C$ фактически пропорциональна $c$ (скаляр $c$ выносится): $c_{21}=c f$, $c_{22}=c g$. Это позволяет вычислить оба этих элемента одним умножением: достаточно найти $c \cdot (f+\lambda g)$ с подходящим коэффициентом $\lambda$, а затем скомбинировать полученное с ещё одним произведением для извлечения отдельных $c_{21}$ и $c_{22}$. Аналогичным образом верхние элементы можно получить сочетанием произведений, использующих общие слагаемые. Естественно, при таком объединении через одно умножение появляются лишние смешанные члены (например, $a,g$ или $b,e$), которые не присутствуют в исходном произведении. В точном алгоритме от них избавиться невозможно, но в приближённом их можно «обнулить» в предельном смысле, подобрав специальные коэффициенты с зависимостью от $\varepsilon$. По сути, выбираются такие линейные комбинации строк $A$ и $B$, что ненужные слагаемые оказываются пропорциональны степеням $\varepsilon$ и уходят при $\varepsilon\to 0$. В результате за 5 умножений удаётся выразить все 4 компоненты $C$ с точностью до $o(1)$ при $\varepsilon\to 0$.

Отметим, что точный ранг этой задачи равен 6 (нетривиальное сокращение до 5 невозможно без погрешности), поэтому найденное решение существенно использует идею предельного перехода. Полученный базовый APA-алгоритм является неустойчивым в обычном смысле: при $\varepsilon\neq 0$ он немного искажает результат, и для высокой точности требуется работать с очень малым $\varepsilon$, что может усиливать влияние ошибок округления. Тем не менее, как шаг анализа такой алгоритм ценен тем, что даёт лучшее асимптотическое число операций.

### 8.2 Алгоритм на 10 умножений для задачи $(3,2,2)$

Следующий этап – «склеить» два алгоритма для специальной структуры $2\times2$ в единый алгоритм для более крупной прямоугольной задачи. Конкретно, рассмотрим задачу умножения матрицы $A'$ размера $3\times 2$ на матрицу $B'$ размера $2\times 2$ с получением $C' = A' B'$ размера $3\times2$. Обозначим эту задачу тройкой $(3,2,2)$ – здесь первая цифра $3$ отвечает числу строк $A'$ (и $C'$), вторая $2$ – числу столбцов $A'$ (и строк $B'$), третья $2$ – числу столбцов $B'$ (и $C'$). Матрица $A'$ размером $3\times2$ содержит, вообще говоря, 6 независимых элементов:

$$A' = \begin{pmatrix} a & b \\ 
c & d \\ 
e & f \end{pmatrix}, \quad B' = \begin{pmatrix} p & q \\
r & s \end{pmatrix}, \quad C' = A'B' = \begin{pmatrix} ? & ? \\
? & ? \\
? & ? \end{pmatrix}.$$

Авторы воспользовались результатом для специального $2\times2$ случая дважды – для верхней и нижней подматрицы $A'$. А именно, они построили алгоритм для $A'$ разбитой по горизонтали на две подматрицы вида $2\times2$: 

$$
\begin{pmatrix} 
a & b\\ 
c & d 
\end{pmatrix}, \quad \begin{pmatrix}
c & d \\ 
e & f 
\end{pmatrix}, где
$$

первая подматрица имеет структуру верхней треугольной (если $d=0$), а вторая – структуру нижней треугольной (если $c=0$). Хотя в $A'$ обе подматрицы перекрываются (элемент $c$ входит в состав обеих), удалось составить комбинированный набор линейных форм, который использует обе схемы на 5 умножений и в совокупности вычисляет произведение $A'B'$ за $5+5=10$ умножений с произвольно малой ошибкой. Идея в том, чтобы применить две конструкции из предыдущего пункта: одну – для верхней части $A'$ (предполагая $d\approx 0$ на первый порядок), вторую – для нижней части $A'$ (предполагая $c\approx 0$ на первый порядок), и скомбинировать их таким образом, чтобы лишние члены от отклонения $c,d$ от нуля компенсировались в первом порядке по $\varepsilon$. В результате получается APA-алгоритм ранга $r=10$ для задачи $(3,2,2)$.

Таким образом, пограничный ранг задачи $(3,2,2)$ не превышает 10. Для сравнения, тривиальный метод требует $3\times 2\times 2 = 12$ умножений, то есть экономия составляет 2 скалярных умножения. В точном режиме задача $(3,2,2)$ не допускает ранга 10 (как считается, её точный ранг равен 11 или 12), но приближённо мы можем достичь 10.

### 8.3 Двойственность и алгоритмы для $(2,3,2)$ и $(2,2,3)$

Задача умножения матриц обладает естественными симметриями. Если существует алгоритм для перемножения матрицы $l\times m$ на $m\times n$, то, применяя принцип двойственности, можно получить алгоритмы для умножения матриц размеров $(m,n)$ на $(n,l)$ и $(n,l)$ на $(l,m)$ с той же сложностью. В теории тензоров это соответствует перестановке индексов тензора ранга. В частном случае результат Бини и др. для $(3,2,2)$ влекёт существование APA-алгоритмов ранга 10 для задач $(2,3,2)$ и $(2,2,3)$. Они соответствуют перемножению, соответственно: (a) матрицы $2\times 3$ на матрицу $3\times 2$ (получая результат $2\times 2$), и (b) матрицы $2\times 2$ на матрицу $2\times 3$ (получая результат $2\times 3$). Оба случая тривиально требуют $2\times3\times2 = 12$ умножений, но с помощью двойственности переносится схема на 10 умножений. В частности, алгоритм для $(2,2,3)$ получается транспонированием роли матриц $A'$ и $B'$ в рассмотренной конструкции: фактически алгоритм для $(3,2,2)$ можно переписать в терминах схемы вычисления элементов по столбцам, тогда он превращается в алгоритм для $(2,2,3)$. Аналогично получается и случай $(2,3,2)$ (транспонированием другой пары измерений тензора). Таким образом, найденный базовый кирпичик с 10 умножениями применим во всех трёх несовпадающих порядках перемножения $2$ и $3$.

Заметим, что наличие алгоритмов для прямоугольных случаев позволяет перемножать неквадратные матрицы быстрее тривиального способа. В контексте оценки экспоненты $\omega$ важен следующий факт: если существует алгоритм ранга $r$ для некоторой задачи $(l,m,n)$, то из него можно построить алгоритм для перемножения квадратных матриц большего размера за $O(n^{\log_{L} r})$ операций, где $L = \max{l,m,n}$. Однако более эффективным часто оказывается приём кронекеровского тензорного произведения нескольких «кирпичиков».

## Построение алгоритма умножения $12\times12$ матриц из полученных блоков (1000 умножений)

Используя алгоритмы для задач $(3,2,2)$, $(2,3,2)$, $(2,2,3)$, авторы составили рекурсивный схемный алгоритм для перемножения достаточно больших квадратных матриц. В качестве целевого примера в работе построен явный APA-алгоритм для умножения двух квадратных матриц размера $12\times 12$ с числом скалярных умножений равным $1000$. Это достигается вложенным применением трёх указанных прямоугольных алгоритмов на уровнях иерархии блоков (трёхступенчатая блоковая структура):

- 1-й уровень (крупные блоки $4\times6$ и $6\times6$). Исходные 12x12 матрицы $A$ и $B$ разбиваются на блоки так, чтобы на уровне блоков задача имела формат $(3,2,2)$. А именно, разобьём $A$ на 3 горизонтальных полосы по 4 строки (итого $3$ блок-строки) и 2 вертикальных полосы по 6 столбцов ($2$ блок-столбца), а матрицу $B$ – на 2 горизонтальных блока по 6 строк и 2 вертикальных блока по 6 столбцов. Тогда на уровне блоков $A$ имеет размерность $3\times 2$, $B$ – $2\times 2$, и можно применить APA-алгоритм для $(3,2,2)$. Этот алгоритм выполнит $10$ умножений блоков $4\times 6$ на блоки $6\times 6$ (каждое умножение здесь – уже не скаляр, а произведение двух матриц соответствующих размеров). После перемножения $10$ пар блоков и сложения результатов получится матрица $C = A\cdot B$ размером $12\times 12$. Благодаря приближённости алгоритма итог $C$ будет получен с небольшими ошибками порядка $\varepsilon$.
- 2-й уровень (средние блоки $2\times2$ и $2\times3$). Теперь требуется выполнить $10$ умножений вида: блок $4\times6$ матрицы $A$ на блок $6\times6$ матрицы $B$. За каждый такой блочный “субзадачи” отвечает прямоугольная задача формата $(4,6,6)$ (произведение $4\times 6$ на $6\times 6$). Чтобы выполнить их быстрее, применим к каждой такой паре алгоритм для $(2,3,2)$. Для этого разобьём каждый соответствующий блок $4\times6$ (из $A$) на 2 горизонтальные полосы по 2 строки и 3 вертикальные полосы по 2 столбца – на уровне этих средних блоков $A$ приобретает размер $2\times 3$. Соответствующий блок $6\times6$ (из $B$) разбивается на 3 горизонтальные полосы по 2 строки и 2 вертикальные полосы по 3 столбца – на уровне средних блоков $B$ имеет размер $3\times 2$. Таким образом, для каждой из 10 пар блоков формируется задача формата $(2,3,2)$ на уровне средних блоков. Из предыдущего пункта мы располагаем APA-алгоритмом ранга 10 для этой задачи, поэтому умножение каждого блока $4\times6$ на соответствующий блок $6\times6$ можно выполнить за 10 умножений малых блоков $2\times2$ на $2\times3$. В результате второй уровень даст $10\times 10 = 100$ умножений малых блоков.
- 3-й уровень (малые блоки $1\times1$). Осталось выполнить умножения формата $(2,2,3)$: каждое из 100 умножений второго уровня представляет задачу перемножения блока $2\times2$ на блок $2\times3$. На этом малом уровне мы делим каждый $2\times2$-блок $A$ на 2 строки по 1 элемент (т.е. 2 блок-строки) и 2 столбца по 1 элемент (2 блок-столбца), а соответствующий $2\times3$-блок $B$ – на 2 строки по 1 элемент и 3 столбца по 1 элемент. Тогда на уровне скалярных элементов задача имеет формат $(2,2,3)$ и для неё применим APA-алгоритм ранга 10 (двойственный случаю $(3,2,2)$). Этот алгоритм выполнит 10 умножений чисел (элементов матриц) вместо 12. Иными словами, каждый малый блок $2\times2$ умножается на $2\times3$ с помощью 10 скалярных умножений вместо 12.

В итоге общее число скалярных умножений в построенном алгоритме равно $10 \times 10 \times 10 = 1000$. Таким образом, произведение двух $12\times 12$ матриц удаётся приближённо вычислить за 1000 умножений вместо $12^3 = 1728$ при обычном методе. Эквивалентно, достигается билинейный пограничный ранг $R(12,12,12)\le 1000$.

### 8.4 Оценка экспоненты $\omega \le \log_{12}(1000) \approx 2.7799$

Асимптотическая сложность умножения произвольных $n\times n$ матриц описывается показателем $\omega$, определённым неформально как наименьшее число, для которого существует алгоритм с $O(n^\omega)$ арифметических операций. Конструкция Бини и соавторов сразу дала новую верхнюю оценку на $\omega$. Если мы можем перемножать $12\times12$ матрицы за 1000 операций, то для больших матриц можно применять идею рекурсии (крупные матрицы разбивать на блоки $12\times12$ и использовать алгоритм как процедуру). Формально, из существования алгоритма ранга $N$ для задачи $(k,k,k)$ следует, что $\omega \le \log_k N$. В нашем случае $k=12$, $N=1000$, откуда:

$$\omega \le \log_{12} 1000.$$

Вычислим это значение: $\log_{12} 1000 = \frac{\ln 1000}{\ln 12}$. Заметим, что $1000 = 10^3$, а $12 = e^{\ln 12}$. Приближённо $\ln 1000 = 6.9078$, $\ln 12 = 2.4849$. Отношение составляет $\approx 2.7799$. Таким образом, $\omega \le 2.7799$.

Полученная оценка значительно улучшила прежние результаты. Для сравнения, алгоритм Панна (1978) имел экспоненту $\omega \approx 2.796$, а более ранний алгоритм Штрассена – $\omega \approx 2.8074$. В то же время новый рекорд носил приближённый характер – впервые была показана возможность снизить экспоненту ценой отказа от строго точных вычислений. Спустя несколько лет Шёнхаге (1981) получил уже точный алгоритм с $\omega \approx 2.522$, однако он был довольно сложным. Тем не менее, работа Бини и др. стала концептуальным прорывом: она ввела технику APA-алгоритмов (пограничного ранга) в арсенал средств понижения экспоненты и подтолкнула дальнейшие исследования.

---

**Группа 3 - Теоритические алгоритмы**

## 9. Теорема Шёнхаге(1981 год)

Пусть  F-некоторое поле, коэффициенты $\alpha_{i,j,h,l}$,  $\beta_{j,k,h,l}$, $\gamma_{k,i ,h,l}$ принадлежат $F(\lambda)$ (поле рациональных функций от параметра $\lambda$). Многочлен $f_g$, определенный на этом поле, такой, что 
$$\sum_{l}^{L} \left( \sum_{i,j,h} \alpha_{i,j,h,l}x_{i,j}^{(h)} \right)\left( \sum_{j,k,h} \beta_{j,k,h,l}y_{j, k}^{(h)} \right)\left( \sum_{k,i,h} \gamma_{k,i ,h,l}z_{k, i}^{(h)} \right) =
\sum_{h}\left(\sum_{i}^{m_h}\sum_{j}^{n_h}\sum_{k}^{p_h} x_{i,j}^{(h)}\cdot y_{j, k}^{(h)}\cdot z_{k, i}^{(h)} \right) + \sum_{g < 0} \lambda^{g}f_{g}(x_{i,j}^{(h)}, y_{j, k}^{(h)}, z_{k, i}^{(h)} )$$ 
для всех подходящих переменных.
Тогда для любого $\epsilon > 0$ можно построить алгоритм умножения матриц $N$ на $N$ за $O\left(N^{3\tau+\epsilon}\right)$, где $\tau$ определяется из формулы
$$L = \sum_{h} (m_{h}n_{h}p_{h})^{\tau}$$. $\qquad$ (1)

В данной формуле левая часть представляет собой трилинейное разложение тензора ранга $L$(ранг также обозначают как $R$ и $r$). Иными словами, что если мы можем представить трилинейное разложение тензора умножения матриц $N$ на $N$  ввиде суммы h независимых умножений матриц $\langle m_{h}, n_{h}, p_{h} \rangle$ с некоторой погрешностью $O(\lambda)$, то ранг тензора можно вычислить по формуле (1).

## 10. конструкция Штрассена(1986 год)

Штрассен заметил, что, используя возможность умножения матриц $N$ на $N$, можно "приблизительно" посчитать умножение $\frac{3}{4}N^{2}$ пар независимых скаляров: $\sum_{i = 1}^{\frac{3}{4}N^{2}} x_{i}y_{i}z_{i} + O(\lambda)$, где $x_{i}, y_{i}, z_{i}$ – независимые. $\qquad$ (2)

Выражаем это через матричное умножение и получаем: $\sum_{i+j+k=g} x_{i,j}y_{j,k}z_{k,i} + O(\lambda)$, где $1 \le i,j,k \ge N$, а $g=\[\frac{3}{2}(N+1)\]$. Данные условия гарантируют, что $x_{i, j}$ участвует не более чем в одном произведении. Троек $(i,j,k)$, удовлетворяющих данным условиям, около $\frac{3}{4}N^{2}$. Данная формула называется конструкцией из матрицы в скаляр.

Штрассен используя трилинейное тождество: $\sum_{i=1}^{q}(x_{0}^{\[2\]}+\lambda x_{i}^{\[1\]})(y_{0}^{\[1\]}+\lambda y_{i}^{\[2\]})(z_{i}\lambda^{-1})+x_{0}^{\[2\]}y_{0}^{\[1\]}(-\sum_{i=1}^{q}z_{i}\lambda^{-1}) = \sum_{i=1}^{q}(x_{i}^{\[1\]}y_{0}^{\[1\]}z_{i}+x_{0}^{\[2\]}y_{i}^{\[2\]}z_{i})+ O(\lambda)$. $\qquad$ (3)

Это даёт базовый алгоритм, использующий $q+1$ умножений для вычисления блочного внутреннего произведения: $\sum_{i=1}^{q}(x_{i}^{\[1\]}y_{0}^{\[1\]}z_{i}+x_{0}^{\[2\]}y_{i}^{\[2\]}z_{i})$. $\qquad$ (3)
Верхние индексы здесь обозначают индексы в блоке внутреннего произведения и однозначно определяются нижними индексами. Мы можем обозначить $x_i$ и $x_0$ разными верхними индексами, поскольку это разные переменные; аналогично для $y_i$ и $y_0$. Но переменные $z$ входят в оба блока и разделяются. Это новое усложнение в базовом алгоритме. Сам алгоритм сам по себе не является матричным произведением. (Если попытаться представить его как матричное произведение, то, поскольку все переменные $z_i$ связаны с одной и той же $y$-переменной $y^{[1]}$, у них должен быть одинаковый $k$-индекс. Но поскольку они все связаны с одной и той же $x$-переменной $x^{[2]}$, у них должен быть одинаковый $i$-индекс.) 

Первый блок $z(x^{[1]}, y^{[0]}, z^{[0]})$ представляет собой матричное произведение размера $\langle q,1,1 \rangle$. Столбцовый вектор $x$ (размера $q \times 1$) умножается на скаляр $y_0$ ($1 \times 1$), давая столбцовый вектор $v$ (размера $q \times 1$), который сопряжён с вектором $z$. Во втором блоке $(x^{[0]}, y^{[2]}, z^{[0]})$ матричное произведение имеет размер $\langle 1,1,q \rangle$. Скаляр $x_0$ ($1 \times 1$) умножается на вектор-строку $y$ ($1 \times q$), давая вектор-строку ($1 \times q$) $v$, опять сопряжённый с вектором $z$. Трудность возникает при попытке сложить два блока: индексы $i$ в $v$ «не знают», быть ли им столбцовыми или строковыми. Конструкция Штрассена даёт способ преодоления этой трудности. 
Возьмём ещё две конструкции (3), полученные циклическими перестановками переменных $x,y,z$, и тензорно их перемножим; получится алгоритм, требующий $(q+1)^3$ умножений для вычисления $q^3$ матричных произведений: 

$\sum_{i,j,k=1}^{q} (x_{i,j,0}^{\[1,1\]} y_{0,j,k}^{\[1,1\]} z_{i,0,k}^{\[1,1\]}+x_{i,j,k}^{\[2,1\]} y_{0,j,k}^{\[1,1\]} z_{i,0,0}^{\[1,2\]}+ x_{i,j,0}^{\[1,1\]} y_{0,0,k}^{\[1,2\]} z_{i,j,k}^{\[2,1\]}+ x_{i,j,k}^{\[2,1\]} y_{0,0,k}^{\[1,2\]} z_{i,j,0}^{\[2,2\]}+ x_{0,j,0}^{\[1,2\]} y_{i,j,k}^{\[2,1\]} z_{i,0,k}^{\[1,1\]}+ x_{0,j,k}^{\[2,2\]} y_{i,j,k}^{\[2,1\]} z_{i,0,0}^{\[1,2\]}+ x_{0,j,0}^{\[1,2\]} y_{i,0,k}^{\[2,2\]} z_{i,j,k}^{\[2,1\]}+ x_{0,j,k}^{\[2,2\]} y_{i,0,k}^{\[2,2\]} z_{i,j,0}^{\[2,2\]})+ O(\lambda)$

Данная формула представляет собой блочное умножение матриц 2 на 2(видно по верхним индексам), где каждый блок представляет собой умножение матриц меньшего размера.

Взяв $N$-ю тензорную степень этого алгоритма, получим алгоритм, требующий $(q+1)^{3N}$ умножений и задающий блочное матричное произведение размера $2^N \times 2^N$, где каждый блок будет являться умножением матриц $<m,n,p>$, где $mnp=q^{3N}$. Затем, применив «матрично-скалярную конструкцию» к блочной структуре, мы получим $\frac{3}{4}(2^{N})^2$ независимых матричных произведений, каждое размера $\langle m,n,p \rangle$ (где $mnp=q$). Применяя $\tau$-теорему Шёнхаге, получаем $\omega < 3\tau$ и $(q+1)^{3N}=\frac{3}{4}2^{2N}(q^{3N})^{\tau}$.Берем $N$ корень и получаем $w<\log_{q} \frac{(q+1)^3}{4}$ При $q=5$ Штрассен получает $\omega < \log_5(63/22) \approx 2.4785$.

## 11. Теорема Салема-Спенлер

Теорема (Салем–Спенсер). Для любого $\varepsilon > 0$ существует конечное число $M_\varepsilon$ такое, что для всех $M > M_\varepsilon$ найдётся множество $B$ из $M' > M^{1-\varepsilon}$ различных целых чисел
$0<b_1<b_2<\cdots<b_{M'}<\frac{M}{2}$

в котором нет трёх членов арифметической прогрессии. Это означает, что для любых $b_i, b_j, b_k \in B$ равенство $b_i + b_j = 2b_k$ (по модулю $M$) выполняется тогда и только тогда, когда $b_i = b_j = b_k$.

Мы будем рассматривать кольцо $\mathbb{Z}/M\mathbb{Z}$ целых чисел по модулю $M$, где $M$ нечётно. Поскольку элементы множества Салема–Спенсера удовлетворяют $0 < b_i < M/2$, ни три из них не могут образовать арифметическую прогрессию по модулю $M$. То есть для любых $b_i, b_j, b_k \in \mathbb{Z}/M\mathbb{Z}$ выполнение
$b_i+b_j−2b_k≡0$ (mod $M$) $b_i​+b_j−2b_k ≡0$(mod $M$)   
возможно тогда и только тогда, когда $b_i = b_j = b_k$. $\qquad$ (4)

## 12. Алгоритм Копперсмита-Винограда(1990 год)

### 12.1 Упрощённый вариант

Копперсмит и Виноградов расширяют алгоритм Штрассера, используя q+2 умножений: $\sum_{i=1}^{q}(x_0^{\[0\]}y_i^{\[1\]}z_i^{\[1\]}+x_i^{\[1\]}y_0^{\[0\]}z_i^{\[1\]}+x_i^{\[1\]}y_i^{\[1\]}z_0^{\[0\]})+O(\lambda)$. $\qquad$ (5)

Пусть $x$-переменные разбиваются на два блока: $X^{\[0]} = {x_0^{\[0]}}$ и $X^{[1]} = {x_i^{[1]} \mid i = 1, \dots, q}$. Аналогично $y$-переменные разбиваются на блоки $Y^{[0]}$ и $Y^{[1]}$, а $z$-переменные – на блоки $Z^{[0]}$ и $Z^{[1]}$. При «занулении» блока $X^{\[I\]}$ (или $Y^{\[J\]}$, $Z^{\[K\]}$) мы зануляем все $x$- (или $y$-, $z$-) переменные с данным образцом индексов.

Фиксируем $\varepsilon > 0$. Выберем $N$ достаточно большим, чтобы число $M$, определённое ниже, превосходило $M_\varepsilon$ из теоремы Салема–Спенсера. Берём $3N$-ю тензорную степень конструкции (5). Каждая переменная $x_i^{\[I\]}$ в этой тензорной степени является тензорным произведением $3N$ переменных $x_j^{\[J\]}$, по одной из каждой из $3N$ копий исходного алгоритма . Её нижний индекс $i$ станет вектором длины $3N$ из ${0, 1, \dots, q}$, составленным из соответствующих нижних индексов, а верхний индекс $\[I\]$ – вектором длины $3N$ из ${0, 1}$, составленным из соответствующих старших индексов (0 или 1). Как и прежде, $\[I\]$ однозначно определяется $i$.

Установим в ноль все переменные $x_i^{\[I\]}$, для которых в векторе $\[I\]$ не ровно $N$ нулей и $2N$ единиц; аналогично обработаем $y$- и $z$-переменные. Пусть $M = 2 \binom{3N}{N} + 1$. Построим множество Салема–Спенсера $B$. Выберем случайные целые $0 \leq w_j < M$, $j = 0, 1, \dots, 3N$. Для каждого верхнего индекса $I \in {0, 1}^{3N}$ вычислим хеш следующим образом: для каждой позиции $j = 1, \dots, 3N$ пусть $I_j$ – $j$-й компонент вектора $I$ (0 или 1). Определим:

$b_X(I) \equiv \sum_{j=1}^{3N}I_jw_j \pmod{M}$

$b_Y(J) \equiv w_0 + \sum_{j=1}^{3N}J_jw_j \pmod{M}$

$b_Z(K) \equiv \frac{1}{2}(w_0 +  \sum_{j=1}^{3N}(2-K_j)w_j \pmod{M}$

Заметим, что для любых блоков $X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]}$, чьё произведение входит в тензорную форму, выполняется $b_X(I)+b_Y(J)-2b_Z(K) \equiv 0 \pmod{M}$ $\qquad$ (6)

Это следует из учёта вклада каждого $w_j$ в суммы. Установим в ноль все блоки $X^{\[I\]}$ с $b_X(I) \notin B$, блоки $Y^{\[J\]}$ с $b_Y(J) \notin B$ и блоки $Z^{\[K\]}$ с $b_Z(K) \notin B$. Тогда для любого оставшегося произведения $X^{\[I\]}Y^{\[J\]}Z^{\[K\]}$ имеем $b_X(I) + b_Y(J) \equiv 2b_Z(K) \pmod{M}$ и $b_X(I), b_Y(J), b_Z(K) \in B$, откуда по свойствам $B$ следует $b_X(I)=b_Y(J)=b_Z(K)$.

Для каждого $b \in B$ составляем список троек $(X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]})$ совместимых ненулевых блоков, у которых $b_X(I) = b_Y(J) = b_Z(K) = b$. Блок $X^{\[I\]}$ представляет собой множество $q^{2N}$ переменных $x_j^{\[J\]}$ с ненулевыми индексами в двух наборах по $2N$ мест, то есть разделённых общим верхним индексом $I$. Ненулевым блоком называется блок, который ещё не был занулён. Блоки $X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]}$ называются совместимыми, если их нулевые индексы не пересекаются попарно. Для каждой тройки $(X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]})$ в списке: если она делит какой-либо блок (скажем, $Z^{\[K\]}$) с более ранней тройкой, то зануляем один из других блоков (скажем, $Y^{\[J\]}$), тем самым устраняя эту тройку. (Если каждый из $X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]}$ встречался в предыдущих троиках, то, зануляя один блок, мы удалим по крайней мере две тройки.)

Для фиксированного $b \in B$ ожидаемое число троек в списке до удаления равно $\binom{3N}{N,N,N}M^{-2}$.

Здесь $\binom{3N}{N,N,N}$ – число совместимых троек $(X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]})$, а множитель $M^{-2}$ – вероятность независимых событий $b_X(I) = b$ и $b_Y(J) = b$ (если оба выполнены, то автоматически $b_Z(K) = b$). Иными словами, для фиксированных $X^{\[I\]}, Y^{\[J\]}$ и целого $b \pmod{M}$, при случайном выборе $w_j$ имеем $\Pr(b_X(I) = b) = \Pr(b_Y(J) = b) = M^{-1}$, так как суммы затрагивают разные $w_j$. Суммируя эти вероятности по всем возможным тройкам, получаем исходное ожидание. Нам не нужна независимость между разными тройками, так как мат. ожидание суммы равно сумме мат. ожиданий независимо от зависимости.

Ожидаемое число неупорядоченных пар ${(X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]}), (X^{\[I'\]}, Y^{\[J'\]}, Z^{\[K\]})}$, имеющих общий $Z$-блок, равно примерно $\frac{1}{2}\binom{3N}{N,N,N}(\binom{2N}{N,N}-1)M^{-3}$.

Здесь $\binom{2N}{N, N} - 1$ – число других $Y$-блоков, совместимых с данным $Z^{\[K\]}$ (кроме $Y^{\[J\]}$). Фактор $1/2$ устраняет двойной подсчёт пар. Множитель $M^{-3}$ – вероятность независимых событий $b_Z(K) = b, b_Y(J) = b, b_Y(J') = b$, которые остаются независимыми даже если $J' = J$ (из-за случайного смещения $w_0$). Аналогично вычисляются пары, разделяющие $Y$- или $X$-блок.

Предположим, мы зануляем блок $Y^{\[J'\]}$ по причине пары троек, разделяющих $Z$-блок. Если $L$ троек (ещё не удалённых) делят этот $Y^{\[J'\]}$, то обнуление $Y^{\[J'\]}$ убьёт все эти $L$ троек, при этом удаляется по меньшей мере $\binom{L}{2} + 1 > L$ пар (все пары троек, деливших $Y^{\[J\]}$, и хотя бы одна пара с общим $Z$-блоком). Значит, число удалённых пар не меньше числа удалённых троек. Таким образом, получается оценка:

Лемма. Ожидаемое число троек, оставшихся в каждом списке после удаления, не меньше $\binom{3N}{N,N,N}M^{-2}-\frac{3}{2}\binom{3N}{N,N,N}(\binom{2N}{N,N}-1)M^{-3} \ge \frac{1}{4} \binom{3N}{N,N,N}M^{-2}$ $\qquad$ (7)

Ожидаемое число троек во всех списках после удаления не меньше $H \equiv \frac{1}{4} M'\binom{3N}{N,N,N}M^{-2}$ $\qquad$ (8)

Это ожидание по выбору $w_j$. Существует выбор $w_j$, дающий не менее $H$ троек; зафиксируем этот выбор.

Наш алгоритм вычисляет как минимум $H$ блочных скалярных произведений $X^{\[I\]}Y^{\[J\]}Z^{\[K\]}$. Каждое такое произведение свободно представляет собой матричное произведение размера $\langle q^{N}, q^{N}, q^{N} \rangle$, причём все переменные не пересекаются (свойство Салема–Спенсера). Применяя $\tau$-теорему Шёнхаге, получаем оценку для $\omega$: $w \le 3\tau \quad (q+2)^{3N} \ge \frac{1}{4} M'\binom{3N}{N,N,N}M^{-2}q^{3N\tau}$.

В частности, используя формулу Стирлинга и пределы $\varepsilon \to 0$, $N \to \infty$, получаем $(q+2)^3 \le \frac{27}{4}q^{3\tau}$.

Минимизируем и получаем при $q=8$: $w \ge \log_8(4000/27) \approx 2.40364$.  $\qquad$ (9)

### 12.2 Связывание весов

До сих пор мы предполагали, что веса $w_j$ независимы. В этом разделе мы сделаем их зависимыми: по существу положим $w_{2j-1} = w_{2j}$. Это требует пересчёта случайных аргументов, но позволяет получить лучшие оценки «ценности» трёхлинейной формы.

Введём понятие «ценность» ($V_r$) трёхлинейной формы $A$. Предположим матричный показатель $\omega = 3r$. Если тензорная степень $A^{\otimes N}$ может быть сведена (подстановкой переменных) к параллельному вычислению нескольких матричных произведений $m_h, n_h, p_h$, то определим $V_\tau (A) \ge (\sum_h (m_hp_hn_h)^\tau)^{\frac{1}{N}})$.

Симметризуем: если $\pi$ – циклическая перестановка переменных $(x, y, z)$, то положим $V_\tau (A) = (V_{\tau}(A \otimes \pi A \otimes \pi^2 A))^{\frac{1}{3}}$. Тогда «ценность» удовлетворяет супермультипликативности $V_r(A \otimes B) \geq V_r(A) V_r(B)$ и суперадаптивности $V_r(A \oplus B) \geq V_r(A) + V_r(B)$. Кроме того, если $A$ сводится к $B$, то $V_r(A) \geq V_r(B)$.

Теперь возьмем базовый алгоритм с небольшим изменением: $\sum_{i=1}^{q} \Big(x_{0}^{\[0\]} y_{i}^{\[1\]} z_{i}^{\[1\]}+ x_{i}^{\[1\]} y_{0}^{[0\]}z_{i}^{[1\]}+x_{i}^{[1\]}y_{i}^{\[1\]} z_{0}^{\[0\]}\Big)+ x_{0}^{\[0\]} y_{0}^{\[0\]} z_{q+1}^{\[2\]}+ x_{0}^{\[0\]} y_{q+1}^{\[2\]} z_{0}^{\[0\]}+ x_{q+1}^{\[2\]} y_{0}^{\[0\]} z_{0}^{\[0\]}+ O(\lambda)$. $\qquad$ (10)

Данную конструкция называется тензором Копперсмита-Винограда($CW_q$).

Начнём с тензорного квадрата конструкции (10), но переобозначим верхние индексы (перегруппируем переменные). Тогда получаем

$(q+2)^2 \to \sum_{i,k=1}^{q}(x_{0,0}^{\[0\]}\,y_{i,k}^{\[2\]}\,z_{i,k}^{\[2\]} + x_{0,k}^{\[1\]}\,y_{i,0}^{\[1\]}\,z_{i,k}^{\[2\]} + x_{0,k}^{\[1\]}\,y_{i,k}^{\[2\]}\,z_{i,0}^{\[1\]} + x_{i,0}^{\[1\]}\,y_{0,k}^{\[1\]}\,z_{i,k}^{\[2\]} + x_{i,k}^{\[2\]}\,y_{0,0}^{\[0\]}\,z_{i,k}^{\[2\]} + x_{i,k}^{\[2\]}\,y_{0,k}^{\[1\]}\,z_{i,0}^{\[1\]} + x_{i,0}^{\[1\]}\,y_{i,k}^{\[2\]}\,z_{0,k}^{\[1\]} +x_{i,k}^{\[2\]}\,y_{i,0}^{\[1\]}\,z_{0,k}^{\[1\]} + x_{i,k}^{\[2\]}\,y_{i,k}^{\[2\]}\,z_{0,0}^{\[0\]}) +$ 

$+ \sum_{i=1}^{q}(x_{0,q+1}^{\[2\]}\,y_{i,0}^{\[1\]}\,z_{i,0}^{\[1\]} + x_{0,0}^{\[0\]}\,y_{i,q+1}^{\[3\]}\,z_{i,0}^{\[1\]} + x_{0,0}^{\[0\]}\,y_{i,0}^{\[1\]}\,z_{i,q+1}^{\[3\]} + x_{i,q+1}^{\[3\]}\,y_{0,0}^{\[0\]}\,z_{i,0}^{\[1\]} + x_{i,0}^{\[1\]}\,y_{0,q+1}^{\[2\]}\,z_{i,0}^{\[1\]} + x_{i,0}^{\[1\]}\,y_{0,0}^{\[0\]}\,z_{i,q+1}^{\[3\]} +x_{i,q+1}^{\[3\]}\,y_{i,0}^{\[1\]}\,z_{0,0}^{\[0\]} +x_{i,0}^{\[1\]}\,y_{i,q+1}^{\[3\]}\,z_{0,0}^{\[0\]} +x_{i,0}^{\[1\]}\,y_{i,0}^{\[1\]}\,z_{0,q+1}^{\[2\]} ) + $ 

$+ \sum_{k=1}^{q}(x_{q+1,0}^{\[2\]}\,y_{0,k}^{\[1\]}\,z_{0,k}^{\[1\]} + x_{q+1,k}^{\[3\]}\,y_{0,0}^{\[0\]}\,z_{0,k}^{\[1\]} + x_{q+1,k}^{\[3\]}\,y_{0,k}^{\[1\]}\,z_{0,0}^{\[0\]} + x_{0,0}^{\[0\]}\,y_{q+1,k}^{\[3\]}\,z_{0,k}^{\[1\]} + x_{0,k}^{\[1\]}\,y_{q+1,0}^{\[2\]}\,z_{0,k}^{\[1\]}  + x_{0,k}^{\[1\]}\,y_{q+1,k}^{\[3\]}\,z_{0,0}^{\[0\]} + x_{0,0}^{\[0\]}\,y_{0,k}^{\[1\]}\,z_{q+1,k}^{\[3\]} + x_{0,k}^{\[1\]}\,y_{0,0}^{\[0\]}\,z_{q+1,k}^{\[3\]} + x_{0,k}^{\[1\]}\,y_{0,k}^{\[1\]}\,z_{q+1,0}^{\[2\]}) +$  

$+ (x_{q+1,q+1}^{\[4\]}\,y_{0,0}^{\[0\]}\,z_{0,0}^{\[0\]} + x_{q+1,0}^{\[2\]}\,y_{0,q+1}^{\[2\]}\,z_{0,0}^{\[0\]} + x_{q+1,0}^{\[2\]}\,y_{0,0}^{\[0\]}\,z_{0,q+1}^{\[2\]} + x_{0,q+1}^{\[2\]}\,y_{q+1,0}^{\[2\]}\,z_{0,0}^{\[0\]} + x_{0,0}^{\[0\]}\,y_{q+1,q+1}^{\[4\]}\,z_{0,0}^{\[0\]} + x_{0,0}^{\[0\]}\,y_{q+1,0}^{\[2\]}\,z_{0,q+1}^{\[2\]}+ x_{0,q+1}^{\[2\]}\,y_{0,0}^{\[0\]}\,z_{q+1,0}^{\[2\]} + x_{0,0}^{\[0\]}\,y_{0,q+1}^{\[2\]}\,z_{q+1,0}^{\[2\]} + x_{0,0}^{\[0\]}\,y_{0,0}^{\[0\]}\,z_{q+1,q+1}^{\[4\]} )$ 
(11)

Разделим $(q+2)^{2}$ $x$-переменных на пять блоков $X^{[0]}, X^{[1]}, X^{[2]}, X^{[3]}, X^{[4]}$. Здесь $X^{\[0\]} = \{ x_{0,0}^{\[0\]} \}, X^{\[1\]} = \{ x_{i,0}^{\[1\]}, x_{0,k}^{\[1\]} \}, X^{\[2\]} = \{ x_{q+1,0}^{\[2\]\}, x_{i,k}^{\[2\]}, x_{0,q+1}^{\[2\]} \}, X^{\[3\]} = \{ x_{q+1,k}^{\[3\]}, x_{i,q+1}^{\[3\]} \}, X^{\[4\]} = \{ x_{q+1,q+1}^{\[4\]} \}$. Индексы $i,k$ пробегают от 1 до $q$. В исходном тензорном квадрате надстрочные индексы были вектором из двух координат, например $x_{i,0}^{\[1,0\]}$, а мы объединили их в единственный индекс, например $x_{i,0}^{\[1\]}$. Обратим внимание, что если в трёхлинейной форме появляется произведение $X^{\[I\]} Y^{\[J\]} Z^{\[K\]}$, то суммарная метка $I+J+K=4$. Таким образом, тройную форму можно представить в блочном виде: $\sum_{I+J+K=4} X^{\[I\]} Y^{\[J\]} Z^{\[K\]}$.

Существует четыре типа слагаемых в этой тройной линейной форме. Их можно перечислить следующим образом:

$X^{\[0\]} Y^{\[0\]} Z^{\[4\]} = x_{i,0}^{\[0\]} y_{0,0}^{\[0\]} z_{q+1,q+1}^{\[4\]}$. Это матричное произведение размера $\langle 1,1,1 \rangle$, его «ценность» равна $1$. Таких типов слагаемых всего три $X^{\[0\]} Y^{\[0\]} Z^{\[4\]}$, $X^{\[0\]} Y^{\[4\]} Z^{\[0\]}$, $X^{\[4\]} Y^{\[0\]} Z^{\[0\]}$;

$X^{\[0\]} Y^{\[1\]} Z^{\[3\]} = \sum_{i=1}^{q} x_{0,0}^{\[0\]} y_{i,0}^{\[1\]} z_{i,q+1}^{\[3\]} + \sum_{k=1}^{q} x_{0,0}^{\[0\]} y_{0,k}^{\[1\]} z_{q+1,k}^{\[3\]}$ (и другие подобные; всего шесть штук: например $X^{\[0\]} Y^{\[1\]} Z^{\[3\]}$, $X^{\[0\]} Y^{\[3\]} Z^{\[1\]}$, $X^{\[1\]} Y^{\[0\]} Z^{\[3\]}$, $X^{\[1\]} Y^{\[3\]} Z^{\[0\]}$, $X^{\[3\]} Y^{\[0\]} Z^{\[1\]}$, $X^{\[3\]} Y^{\[1\]} Z^{\[0\]}$). Это матричное произведение размера $\langle 1,1,2q \rangle$ (т.е. скаляр, умноженный на вектор размера $2q$); его «ценность» равна $(2q)^\tau$;

$X^{\[0\]} Y^{\[2\]} Z^{\[2\]} = x_{0,0}^{\[0\]} y_{q+1,0}^{\[2\]} z_{0,q+1}^{\[2\]}+x_{0,0}^{\[0\]} y_{0,q+1}^{\[2\]} z_{q+1,0}^{\[2\]}+\sum_{i,k=1}^{q} x_{0,0}^{\[0\]} y_{i,k}^{\[2\]} z_{i,k}^{\[2\]}$ (и две аналогичные с циклическими перестановками индексов: $X^{\[2\]} Y^{\[0\]} Z^{\[2\]}$, $X^{\[2\]} Y^{\[2\]} Z^{\[0\]}$). Это матричное произведение размера $\langle 1,1,q^2+2 \rangle$; его «ценность» равна $(q^2+2)^\tau$.

$X^{\[1\]} Y^{\[1\]} Z^{\[2\]} = \sum_{i=1}^{q} x_{i,0}^{\[1\]} y_{i,0}^{\[1\]} z_{0,q+1}^{\[2\]}+\sum_{k=1}^{q} x_{0,k}^{\[1\]} y_{0,k}^{\[1\]} z_{q+1,0}^{\[2\]}+\sum_{i,k=1}^{q} x_{i,0}^{\[1\]} y_{0,k}^{\[1\]} z_{i,k}^{\[2\]}+\sum_{i=1}^{q} x_{0,k}^{\[1\]} y_{i,0}^{\[1\]} z_{i,k}^{\[2\]}$ (и две аналогичные комбинации $X^{\[1\]} Y^{\[2\]} Z^{\[1\]}$, $X^{\[2\]} Y^{\[1\]} Z^{\[1\]}$). Их «ценность» исходит из леммы: при $q>3$ она не меньше $2^{\frac{2}{3}} q^\tau (q^{3\tau}+2)^{1/3}$.


Возьмём $N$-ю тензорную степень конструкции (11), где $N$ делится на 3. Пусть $a_l$, $0 \le l \le 4$, положительные целые числа так, что $\sum_{l=0}^4 a_l = 1$ и $\sum_{l=0}^4 l a_l =\frac{4}{3}$. Пусть $A_l \approx a_l N$ целые так, что $\sum_{l=0}^4 A_l = N$ и $\sum_{l=0}^4 l A_l =\frac{4N}{3}$. Оставим только те блоки $(X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]})$, что # $(j | 1 \le j \le N , I_j = l )  = A_l $, остальные зануляя. 

Пусть $M''$ – число ненулевых троек, содержащих заданный блок $X^{\[I\]}$. Тогда $M''=\sum_{\eta_{l,m,n}} \frac{\prod_{0 \le l \le 4} A_l !}{\prod_{l+m+n=4} \eta_{l,m,n} !}$ $\qquad$ (12), где $\{\eta_{l,m,n}\}$ множество разбиение N: $\sum_{m,n} \eta_{l,m,n} = A_l$, $\sum_{l,n} \eta_{l,m,n} = A_m$ и $\sum_{l,m} \eta_{l,m,n} = A_n$. Ненулевые $\eta_{l,m,n}$ удовлетворяют $l+m+n = 4$ для $0 \le l,m,n \le 4$. Оптимальные распределения (12) даёт $\eta_{l,m,n} = \gamma_{l,m,n}$

$\gamma_{0,0,4} = \gamma_{0,4,0} = \gamma_{4,0,0} = \hat a$

$\gamma_{0,1,3} =\gamma_{0,3,1} = \gamma_{1, 0, 3} =  \gamma_{1,3,0} = \gamma_{3,0,1} = \gamma_{3,1,0} = \hat b$

$\gamma_{0,2,2} = \gamma_{2,0,2} = \gamma_{2,2,0} = \hat c$

$\gamma_{1,1,2} = \gamma_{1,2,1} = \gamma_{2,1,1} = \hat d$  $\qquad$ (13)

$A_0 = 2\hat a + 2\hat b + \hat c$

$A_1 = 2\hat b + 2\hat d$

$A_2 = 2\hat c + \hat d$

$A_3 = 2\hat b$

$A_4 = \hat a$

Положим $M = 2M'' + 1$. Построим множество Салема–Спенсера $B$. Как и прежде, выберем случайные $w_j$ и определим:

$b_X(I) \equiv \sum_{j=1}^{N}I_jw_j \pmod{M}$

$b_Y(J) \equiv w_0 + \sum_{j=1}^{N}J_jw_j \pmod{M}$

$b_Z(K) \equiv \frac{1}{2}(w_0 + \sum_{j=1}^{N}(4-K_j)w_j \pmod{M}$

Оставим только блоки, попадающие в $B$. Тогда для любой оставшейся тройки $X^{\[I\]}, Y^{\[J\]}, Z^{\[K\]}$ снова выполнится $b_X(I) = b_Y(J) = b_Z(K)$.

После удаления повторов остаётся примерно $\binom{N}{A_0, A_1, A_2, A_3, A_4}$ точек блоков. Для большей части этих троек (доля $\sim N'^{-p}$) индексы $j = 1, \ldots, N'$ дают примерно $\gamma(l,m, n)$ вхождений под формы $X^{\[l\]}Y^{\[m\]}Z^{\[n\]}$. Тогда «ценность» каждой тройки приблизительно $\[1\]^{3\hat a} \[(2q)^{\tau}\]^{6 \hat b}\[(q^2+2)^{\tau}\]^{3 \hat c} \[2^{\frac{2}{3}} q^{\tau}(q^{3\tau}+2)^{\frac{1}{3}}\]^{3 \hat d} =(2q)^{6 \hat b \tau}(q^2+2)^{3 \hat c \tau}\[4 q^{3 \tau} (q^{3\tau}+2)\]^{\hat d}$.

Это неравенство сводится к $(q+2)^{2N} \ge N^{-p} \binom{N}{A_0, A_1, A_2, A_3, A_4}(2q)^{6 \hat b \tau}(q^2+2)^{3 \hat c \tau}\[4 q^{3 \tau} (q^{3\tau}+2)\]^{\hat d}$.

Надо выбрать $\hat a$, $\hat b$, $\hat c$ и  $\hat d$ так, чтобы максимизировать правую часть. Из (13) следует: $3\hat a + 6\hat b + 3\hat c +3\hat d = N$. Тогда положим, что $\hat a = N\bar a$, $\hat b = N\bar b$, $\hat c = N\bar c$ и $\hat d = N\bar d$,  устремим $N$ в бесконечность и возьмем $N$-ый корень. Получим

$(q+2)^2 = \frac{(2q)^{6 \bar b \tau}(q^2+2)^{3 \bar c \tau}\[4 q^{3 \tau} (q^{3\tau}+2) \]^{\bar d}}{(2 \bar a +2 \bar b + \bar c)^{2 \bar a +2 \bar b + \bar c}(2 \bar b + 2 \bar d)^{2 \bar b + 2 \bar d}(2 \bar c +  \bar d)^{2 \bar c +  \bar d}(2 \bar b)^{2 \bar b}(\bar a)^{\bar a}}$

Минимизируем $\tau$ через $\bar a$, $\bar b$, $\bar c$, $\bar d$ и $q$, соблюдая условия: $3\bar a + 6\bar b + 3\bar c +3\bar d = 1$, $\bar a, \bar b, \bar c, \bar d >0$, $q \ge 3$ и $q \in \mathbb{Z}$.

При $\bar a = 0.000233$, $\bar b = 0.012506$, $\bar c = 0.102546$, $\bar d = 0.205542$ и $q=6$ получаем $w \le 3\tau < 2.375477$.

## 13. Последующие конструкции 

Все методы позже строились над различными преобразованиями $CW_q$.

В 2023(и в последующем улучшении в 2024) рассматривалось множество $CW_q^{\times 2^{L-1}}$, в отличие от прошлых методов, где просто рассматривался $CW_q$ в какой-то степени. 

В работе Williams, Xu, Xu, и Zhou было замечено, что на $l$ уровне можно не обнулять часть троек с повторяющимся блоком $Z^{\[K\]}$(как мы это делали в параграфе 12.1), потому что возможно, что на $l-1$ данные тройки будут использовать разные части  $Z^{\[K\]}$ блока на $l$ уровне(исходит из  того, что все тройки на $l$ уровне являются группировкой троек с $l-1$ уровня). В 2024 году к ним добавились так же Alman и Duan. Они доказали, что такую же вещь можно частично  сделать и с $Y^{\[J\]}$, и тем самым получили на данный момент лучшую оценку $w < 2.371339$.

Для доказательства по факту используются те же методы, что и при доказательстве Коппеерсмита-Винограда, но в более строгом виде. Для обнуления используются маргиналы и распределения, а переход между уровнями $l$ происходит через так называемые interface тензоры. Более подробно об этом написано и в их статье 2024(https://arxiv.org/pdf/2404.16349).


## 14. Вывод по теоретическим методам

Таким образом, теорема Шёнхаге является основной теоремой, на которой в последствии построены все теоретические методы умножения матриц. Возможность находить ранг тензора умножения матриц(и оценку $w$  в том числе), доказав, что тензор умножения некоторой матрицы больше, чем сумма тензоров умножения матриц меньшего размера стала основной в приближение к оценке $O(n^2)$. 


**Вывод**
Роман сделай плиз 





